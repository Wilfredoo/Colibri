import React from 'react';
import * as firebase from 'firebase';
import { Button, Dimensions, Image, StyleSheet, Text, TouchableOpacity, View } from 'react-native';

export default class WatchVideos extends React.Component {
  render() {
    return (
      <View>
        <Text>
          Change code in the editor and waasdatch it change on your phone! Save to get a shareable url.
        </Text>
      </View>
    );
  }
}
